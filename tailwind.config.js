// Locked at publish time. Do not extend without coordinating with
// the FreeWebStore design system — the platform AI assumes vanilla
// Tailwind utilities.
export default {
  content: ["./index.html"],
  theme: { extend: {} },
  plugins: [],
};
